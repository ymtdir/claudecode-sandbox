# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## フェーズ1: 環境セットアップと基盤構築

- [x] 必要なライブラリをインストール
  - [x] react-native-calendarsをインストール
  - [x] react-native-gesture-handlerをインストール
  - [x] react-native-reanimatedをインストール
  - [x] date-fnsをインストール
  - [x] react-native-haptic-feedbackをインストール

- [x] ディレクトリ構造を作成
  - [x] src/screens/Calendar/ディレクトリを作成
  - [x] src/components/calendar/ディレクトリを作成
  - [x] src/utils/calendar/ディレクトリを作成
  - [x] src/mocks/ディレクトリを作成

- [x] モックデータを作成
  - [x] taskData.tsを作成（タスクのモックデータ）
  - [x] 六曜データの定数を作成
  - [x] 祝日データの定数を作成

## フェーズ2: カレンダー表示機能の実装

- [x] MonthViewScreen.tsxを実装
  - [x] 基本的な画面レイアウトを作成
  - [x] 状態管理（選択日付）を追加
  - [x] スタイルファイルを作成

- [ ] CalendarGridコンポーネントを実装
  - [ ] CalendarGrid.tsxを作成
  - [ ] react-native-calendarsの基本設定
  - [ ] カスタムテーマを適用
  - [ ] スタイルファイルを作成

- [ ] 六曜・祝日機能を実装
  - [ ] rokuyou.tsを作成（六曜計算ロジック）
  - [ ] holidays.tsを作成（祝日データ管理）
  - [ ] カレンダーに六曜・祝日を表示

- [ ] タスク数バッジを表示
  - [ ] markedDatesプロパティを設定
  - [ ] カスタムDayComponentを実装
  - [ ] バッジスタイルを適用

## フェーズ3: タスクリストとプログレスバーの実装

- [ ] TodayTaskListコンポーネントを実装
  - [ ] TodayTaskList.tsxを作成
  - [ ] TaskItem.tsxを作成
  - [ ] FlatListでタスクを表示
  - [ ] AddTaskButtonを追加
  - [ ] スタイルファイルを作成

- [ ] ProgressBarコンポーネントを実装
  - [ ] ProgressBar.tsxを作成
  - [ ] 完了率の計算ロジックを実装
  - [ ] アニメーション付きプログレスバーを実装
  - [ ] スタイルファイルを作成

- [ ] コンポーネント間の連携を実装
  - [ ] MonthViewScreenに全コンポーネントを統合
  - [ ] 選択日付に応じたタスク表示
  - [ ] タスク完了率の計算と表示

## フェーズ4: スワイプジェスチャーとアニメーションの実装

- [ ] タスクのスワイプジェスチャーを実装
  - [ ] react-native-gesture-handlerを設定
  - [ ] 右スワイプで完了処理を実装
  - [ ] 左スワイプで延期処理を実装
  - [ ] スワイプアニメーションを追加

- [ ] カレンダーの月切り替えを実装
  - [ ] 左右スワイプで月変更を実装
  - [ ] 月変更時のアニメーションを追加
  - [ ] ヘッダーの矢印ボタンでも月変更

- [ ] ビジュアルフィードバックを実装
  - [ ] タスク完了時のアニメーション
  - [ ] 完了タスクの取り消し線表示
  - [ ] 振動フィードバックを追加（プラットフォーム対応）

## フェーズ5: 統合とテスト

- [ ] 全コンポーネントの統合テスト
  - [ ] カレンダー表示の確認
  - [ ] タスクリストの表示確認
  - [ ] スワイプジェスチャーの動作確認

- [ ] エラーハンドリングを実装
  - [ ] データ取得エラーの処理
  - [ ] 不正な日付の処理
  - [ ] エラーメッセージの表示

- [ ] パフォーマンス最適化
  - [ ] 不要な再レンダリングの防止
  - [ ] React.memoの適用
  - [ ] useMemoの活用

## フェーズ6: 品質チェックと修正

- [ ] すべてのテストが通ることを確認
  - [ ] `npm test`
- [ ] リントエラーがないことを確認
  - [ ] `npm run lint`
- [ ] 型エラーがないことを確認
  - [ ] `npm run typecheck`
- [ ] ビルドが成功することを確認
  - [ ] `npm run build`

## フェーズ7: ドキュメント更新

- [ ] README.md を更新（必要に応じて）
- [ ] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:
- {計画時には想定していなかった技術的な変更点}
- {実装方針の変更とその理由}

**新たに必要になったタスク**:
- {実装中に追加したタスク}
- {なぜ追加が必要だったか}

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- {タスク名}
  - スキップ理由: {具体的な技術的理由}
  - 代替実装: {何に置き換わったか}

**⚠️ 注意**: 「時間の都合」「難しい」などの理由でスキップしたタスクはここに記載しないこと。全タスク完了が原則。

### 学んだこと

**技術的な学び**:
- {実装を通じて学んだ技術的な知見}
- {新しく使った技術やパターン}

**プロセス上の改善点**:
- {タスク管理で良かった点}
- {ステアリングファイルの活用方法}

### 次回への改善提案
- {次回の機能追加で気をつけること}
- {より効率的な実装方法}
- {タスク計画の改善点}