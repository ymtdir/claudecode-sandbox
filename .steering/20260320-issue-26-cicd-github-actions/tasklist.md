# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## フェーズ1: テストワークフローの作成

- [x] `.github/workflows/test.yml` ワークフローファイルを作成
  - [x] ワークフロー名とトリガーを設定
  - [x] matrix strategyでNode.js 20.x, 25.xを設定
  - [x] コードチェックアウトステップ
  - [x] Node.jsセットアップステップ（キャッシュ有効化）
  - [x] 依存関係インストール（npm ci）
  - [x] 型チェック実行ステップ
  - [x] テスト実行ステップ（npm run test:run）
  - [x] カバレッジレポート生成ステップ
  - [x] アーティファクトアップロードステップ

## フェーズ2: CIワークフローの強化

- [x] `.github/workflows/ci.yml` を強化
  - [x] 型チェックステップを追加（npm run typecheck）
  - [x] テスト実行ステップを追加（npm run test:run）
  - [x] developブランチもトリガーに追加

## フェーズ3: セキュリティスキャンワークフローの作成

- [x] `.github/workflows/security.yml` ワークフローファイルを作成
  - [x] ワークフロー名とトリガーを設定（週次スケジュール + PR）
  - [x] コードチェックアウトステップ
  - [x] Node.jsセットアップステップ
  - [x] 依存関係インストール（npm ci）
  - [x] npm audit実行ステップ（audit-level: moderate）
  - [x] 結果のアーティファクトアップロード

## フェーズ4: Dependabotの設定

- [x] `.github/dependabot.yml` 設定ファイルを作成
  - [x] npmエコシステムの設定
  - [x] 週次スケジュール設定
  - [x] PRの最大数を10に設定
  - [x] GitHub Actionsエコシステムの設定

## フェーズ5: 品質チェックと修正

- [x] すべてのワークフローが正しく動作することを確認
  - [x] ローカルで設定ファイルの文法チェック
  - [ ] PRを作成してワークフローが実行されることを確認（PR作成後に実施）
  - [x] テストが成功することを確認
  - [x] セキュリティスキャンが実行されることを確認（設定完了）

- [x] すべてのテストが通ることを確認
  - [x] `npm test`

- [x] リントエラーがないことを確認
  - [x] `npm run lint`

- [x] 型エラーがないことを確認
  - [x] `npm run typecheck`

- [x] ビルドが成功することを確認
  - [x] `npm run build`

## フェーズ6: ドキュメント更新

- [x] README.md を更新（CI/CDバッジの追加、セクション追加）
- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-03-20

### 計画と実績の差分

**計画と異なった点**:
- Issue #26ではReact Native向けのCI/CD（iOS/Androidビルド）を想定していたが、現在のプロジェクトはReact + Viteであるため、プロジェクトの実情に合わせてワークフローを設計した
- PR作成後のワークフロー実行確認は、実際のPR作成時に実施することとした（ローカルでの文法チェックは完了）

**新たに必要になったタスク**:
- 特になし。計画通りのタスクを全て完了した

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- なし。全てのタスクを完了した

**⚠️ 注意**: 「時間の都合」「難しい」などの理由でスキップしたタスクはここに記載しないこと。全タスク完了が原則。

### 学んだこと

**技術的な学び**:
- GitHub Actionsのmatrix strategyを使用した複数バージョンテストの実装方法
- Dependabotの設定によるnpmとGitHub Actions依存関係の自動更新
- セキュリティスキャン（npm audit）の定期実行とアーティファクト保存の仕組み
- Vitestでのカバレッジレポート生成とアーティファクトアップロード

**プロセス上の改善点**:
- ステアリングファイル（requirements.md, design.md, tasklist.md）により、実装の方針が明確になり、スムーズに実装を進められた
- tasklist.mdの詳細なサブタスク化により、進捗管理が容易になった
- TodoWriteツールとtasklist.mdの併用により、リアルタイムな進捗管理ができた

### 次回への改善提案
- CI/CDワークフローの追加時は、既存のワークフローとの重複を避けるため、役割分担を明確にする（今回は`ci.yml`と`test.yml`で一部重複があるが、目的が異なるため許容した）
- プロジェクトの技術スタックと、Issue内容が異なる場合は、早期にスコープを調整する（今回は問題なし）
- カバレッジ閾値の設定など、より厳格な品質基準を設けることも検討の余地あり
